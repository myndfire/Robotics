{
    "name": "ConfigStore",
    "version": "1.0.0",
    "description": "Typed NVS configuration manager for device settings (String, int, bool, float). Composes StorageController + DisplayController. Provides serial command interface and OLED live summary. Keys persist across power cycles.",
    "authors": [{ "name": "MoistureMonitorAgent" }],
    "frameworks": ["arduino"],
    "platforms": ["espressif32"],
    "dependencies": {
        "olikraus/U8g2": "*"
    }
}
